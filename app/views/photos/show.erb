<%= link_to "Back to album", photo_album_path(@photo.photo_album) %>
<%= image_tag @photo.image.url %>
<%= button_to "Delete", delete_photo_path(@photo), method: :delete, data: { confirm: "Are you sure?" }  %>
